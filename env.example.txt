# Application Configuration
NEXT_PUBLIC_APP_NAME=PACSFR
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api

# Logging
NEXT_PUBLIC_LOG_LEVEL=info

# AWS S3 Configuration
# Lấy credentials từ AWS Console: https://console.aws.amazon.com/iam/
NEXT_PUBLIC_AWS_REGION=ap-southeast-1
NEXT_PUBLIC_AWS_ACCESS_KEY_ID=your_access_key_id_here
NEXT_PUBLIC_AWS_SECRET_ACCESS_KEY=your_secret_access_key_here

# S3 Bucket
NEXT_PUBLIC_AWS_S3_BUCKET_NAME=pacsfr-uploads
NEXT_PUBLIC_AWS_S3_BUCKET_URL=https://pacsfr-uploads.s3.ap-southeast-1.amazonaws.com

# AWS Server-side (Optional - for server-side operations)
AWS_ACCESS_KEY_ID=your_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_secret_access_key_here

# Note: 
# - Copy file này thành .env.local để sử dụng
# - KHÔNG commit .env.local vào git
# - Cần tạo IAM User với quyền S3 và lấy credentials
# - Cần tạo S3 Bucket và cấu hình CORS + Public Access (nếu cần)

