export enum ChatRole {
    USER = "user",
    ASSISTANT = "assistant",
}

export enum ChatStatus {
    IDLE = "idle",
    SENDING = "sending",
    RECEIVING = "receiving",
    ERROR = "error",
}

export enum SessionStatus {
    ACTIVE = "active",
    ARCHIVED = "archived",
    DELETED = "deleted",
}

